apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: my-label-1
    steps:
      - name: Say hello 1
        shell: sh
        run: |
          echo "Hello world! I'm $(whoami) on $(hostname)"
      - name: Say hello 2
        shell: sh
        run: |
          echo "Hello world! I'm $(whoami) on $(hostname)"
      - name: Say hello 3
        shell: sh
        run: |
          echo "Hello world! I'm $(whoami) on $(hostname)"
