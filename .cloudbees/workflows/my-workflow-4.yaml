apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  workflow_dispatch:
metadata:
  stages/v1alpha1:
    - name: Stage 1
      jobs:
        - job1
        - jobb2
jobs:
  job1:
    steps:
      - run: echo "hello world"
        uses: docker://golang:1.20.3-alpine3.17
        name: Hello world
        shell: sh
  jobb2:
    needs:
      - job1
    runs-on:
      - my-label-1
      - my-label-2
      - asdfg
    steps:
      - uses: docker://golang:1.20.3-alpine3.17
        shell: sh
