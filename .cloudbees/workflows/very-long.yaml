apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Very long
on:
  workflow_dispatch:
jobs:
  verylong:
    runs-on: my-label-1
    steps:
      - name: Say hello 1
        shell: sh
        run: |
          sleep 6m
