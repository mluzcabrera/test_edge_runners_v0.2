apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Very long
on:
  workflow_dispatch:
jobs:
  verylong:
    runs-on: my-label-1
    needs: build
    steps:
      - name: Say hello 1
        shell: sh
        run: |
          sleep 6m